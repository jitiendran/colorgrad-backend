type Query {
    get_followers(data: friendInput!): [friends]!
    get_following(data: friendInput!): [friends]!
}

input friendInput {
    Id: String!
}

type friends {
    _id: String!
    Username: String!
    Email: String!
    Profile: String
}
